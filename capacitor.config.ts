import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.barcode.basf',
  appName: 'barcode_2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
